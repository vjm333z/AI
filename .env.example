# .env.example — 이 파일을 .env로 복사해서 값을 채우세요
# cp .env.example .env

# === 자바 앱 (rag-app) — 인덱싱용 PMS DB ===
DB_URL=jdbc:mariadb://211.34.228.178:3336/PMS
DB_USERNAME=DEV
DB_PASSWORD=

# === python-svc (STT) — STT 저장용 운영 PMS DB ===
STT_DB_HOST=211.34.228.191
STT_DB_PORT=3336
STT_DB_USER=peh0322
STT_DB_PASSWORD=
STT_DB_NAME=PMS

# === LLM API 키 ===
GROQ_API_KEY=
GROQ_STT_API_KEY=             # 비워두면 GROQ_API_KEY 사용
GROQ_LLM_API_KEY=             # 비워두면 GROQ_API_KEY 사용
OPENAI_API_KEY=
LLM_PROVIDER=openai           # python-svc STT 요약: openai | groq

# === 서비스 포트 (옵션) ===
# SERVER_PORT=8080

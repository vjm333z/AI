# RecallAI Python 통합 서비스 — reranker + STT 전용 슬림 의존성
# torch CPU 버전은 Dockerfile에서 별도 설치 (이미지 크기 ~1.2GB 절약)

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# Reranker
sentence-transformers==3.3.1
# torch는 Dockerfile에서 CPU 빌드로 별도 설치

# STT — Groq Whisper API 호출
requests>=2.31.0
